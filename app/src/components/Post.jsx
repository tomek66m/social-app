import React from "react";
import PropTypes from "prop-types";
import { PostWrapper } from "../styles/post-styling";

const Post = (props) => {
  return <PostWrapper className="custom_box_shadow"></PostWrapper>;
};

Post.propTypes = {};

export default Post;
