import React from "react";
import PropTypes from "prop-types";
import { ContactWrapper } from "../styles/contact-styling";

const Contact = (props) => {
  return <ContactWrapper></ContactWrapper>;
};

Contact.propTypes = {};

export default Contact;
